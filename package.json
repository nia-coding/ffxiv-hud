{
  "name": "ffxiv-hud",
  "version": "0.1.0",
  "description": "FFXIV-style desktop duty list HUD for macOS.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check src/renderer.js"
  },
  "devDependencies": {
    "electron": "^32.0.0"
  }
}
